{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Kerdos desktop main window. W4 will extend this with file-system / dialog permissions as needed.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
