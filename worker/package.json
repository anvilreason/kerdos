{
  "name": "kerdos-quote-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Kerdos 行情中继层 Cloudflare Worker — 解决浏览器 CORS + 多源降级 + KV 缓存",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.0",
    "zod": "^3.23.8"
  }
}
